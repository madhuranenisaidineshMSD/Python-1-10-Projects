# Python Screenshot Tool

A simple Python project that captures the full computer screen and automatically saves the screenshot.

## Features

- Captures the full screen
- Automatically creates a `Screenshots` folder
- Automatically generates a filename
- Saves screenshots as PNG files

## Requirements

Install the required packages:

```bash
python -m pip install pyscreenshot
python -m pip install Pillow